osx_image: xcode10.1
language: objective-c
branches:
  only:
    - master
    - develop
notifications:
  email: false
before_install:
  - gem install bundler
  - bundle install
  - npm install bumped -g
before_script:
  - bumped release patch
script: bundle exec fastline build
